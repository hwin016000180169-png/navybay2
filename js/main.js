gsap.registerPlugin(ScrollTrigger);

// const lenis = new Lenis()

// // 스크롤 이벤트 감지 (선택사항)
// lenis.on('scroll', (e) => {
//   // console.log(e) // 디버깅용
// })

// // 애니메이션 프레임으로 부드럽게 업데이트
// function raf(time) {
//   lenis.raf(time)
//   requestAnimationFrame(raf)
// }
// requestAnimationFrame(raf)